
from __future__ import annotations

def web_search(*args, **kwargs):
    parameters = kwargs.get("parameters") or (args[0] if args else {}) or {}
    return "web_search is available as a safe macOS placeholder. Full automation code was not included in the uploaded files."
